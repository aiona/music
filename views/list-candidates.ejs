<!DOCTYPE html>
<html>
  <head>
    <title>List of Albums</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <a href="/"><h1>Election Data</h1></a>
    <% if (items.length > 0) { %>
    <ul>
      <% items.forEach(function(item) { %>
      <li><a href="/albums/<%=item._id%>"><%=item.name%></a></li>
      <% }); %>
    </ul>
    <% } else { %>
    <div>No candidates found.</div>
    <% } %>
    <div class="control">
      <h2>Search for albums by title</h2>
      <form method="get" action="/candidates/">
        <label>Album Title: <input name="name"></label>
        <button>Search</button>
      </form>
    </div>
  </body>
</html>
